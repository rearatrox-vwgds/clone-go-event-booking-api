services:
  user-service:
    build: ./services/user-service
    ports:
      - "${USERSERVICE_PORT}:8080"
    environment:
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - event-booking-api-network

  event-service:
    build: ./services/event-service
    ports:
      - "${EVENTSERVICE_PORT}:8080"
    environment:
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - event-booking-api-network

networks:
  event-booking-api-network:
    driver: bridge
